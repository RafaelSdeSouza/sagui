# Starlet (à trous) decomposition

Starlet (à trous) decomposition

## Usage

``` r
starlet_mask(img, J = 5)
```

## Arguments

- img:

  2D numeric matrix

- J:

  number of scales (\>=1)

## Value

object of class `sagui_starlet`: list(w = list w1..wJ, cJ = coarse)
