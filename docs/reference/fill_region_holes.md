# Fill zero-valued holes inside each labeled region

Fill zero-valued holes inside each labeled region

## Usage

``` r
fill_region_holes(labels)
```

## Arguments

- labels:

  Matrix of integer labels.

## Value

Matrix with enclosed background holes filled per region.
