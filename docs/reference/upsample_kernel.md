# Insert zeros between kernel taps

Insert zeros between kernel taps

## Usage

``` r
upsample_kernel(k, step)
```

## Arguments

- k:

  base kernel (numeric)

- step:

  integer upsampling factor (\>=1)

## Value

upsampled kernel
