% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contourlet_support.R
\name{build_contourlet_mask}
\alias{build_contourlet_mask}
\title{Build a contourlet-like directional support mask from a spectral cube}
\usage{
build_contourlet_mask(
  input,
  collapse_fn = collapse_white_light,
  pretransform = "none",
  combine = c("hybrid", "standalone"),
  base_mask = NULL,
  starlet_J = 5,
  starlet_scales = 2:5,
  include_coarse = FALSE,
  denoise_k = 2.5,
  mode = c("soft", "hard"),
  positive_only = TRUE,
  angles_deg = seq(0, 165, by = 15),
  scales = list(c(4.5, 1.2), c(7.5, 1.8), c(11, 2.6)),
  background_sigma = 8,
  score_low_q = 0.92,
  score_high_q = 0.98,
  contrast_low_q = 0.8,
  contrast_high_q = 0.93,
  max_growth_iter = 8L,
  close_size = 3L,
  open_size = 3L
)
}
\arguments{
\item{input}{3-D array or FITS-like list with \code{imDat}.}

\item{collapse_fn}{Function used to collapse the cube to a 2-D image.}

\item{pretransform}{Optional spectral pretransform applied to the cube before
the white-light collapse.}

\item{combine}{Either \code{"hybrid"} (recommended; start from the starlet support
and extend it directionally) or \code{"standalone"} (use only the directional
support).}

\item{base_mask}{Optional precomputed base mask. When \code{combine = "hybrid"}
and \code{base_mask} is \code{NULL}, the base support is built from
\code{\link[=build_starlet_mask]{build_starlet_mask()}}.}

\item{starlet_J}{Number of starlet scales used when building the hybrid base
support.}

\item{starlet_scales}{Scales to reconstruct for the hybrid base support.}

\item{include_coarse}{Logical; include the coarse plane in the hybrid base
support reconstruction.}

\item{denoise_k}{Optional starlet denoising threshold used in the hybrid base
support.}

\item{mode}{Thresholding mode for the hybrid base support reconstruction.}

\item{positive_only}{Logical; keep only positive reconstruction values in the
hybrid base support.}

\item{angles_deg}{Vector of filter orientations in degrees.}

\item{scales}{List of \verb{(sigma_major, sigma_minor)} pairs defining the
directional filter bank.}

\item{background_sigma}{Smoothing scale used to estimate the local background
before the directional response is measured.}

\item{score_low_q}{Lower quantile used to define the allowed directional
growth region.}

\item{score_high_q}{Upper quantile used to define strong directional seeds.}

\item{contrast_low_q}{Lower quantile used to define the allowed contrast
growth region.}

\item{contrast_high_q}{Upper quantile used to define strong contrast seeds.}

\item{max_growth_iter}{Maximum number of constrained growth iterations.}

\item{close_size}{Size of the binary closing brush.}

\item{open_size}{Size of the binary opening brush.}
}
\value{
A list containing the collapsed image, the optional base support, the
final logical mask, the directional score map, the local contrast map, and
threshold metadata.
}
\description{
This function extends the default starlet support with a directional,
multiscale arm-sensitive support map. The implementation is contourlet-like
rather than a full contourlet transform: it uses a bank of oriented,
elongated Gaussian filters across multiple scales, combines them into a
directional response map, and optionally grows the starlet support into
nearby arm-like pixels.
}
