% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_clumps.R
\name{clump_evidence_table}
\alias{clump_evidence_table}
\title{Summarise evidence for candidate clumps}
\usage{
clump_evidence_table(
  clump_labels,
  score_info,
  min_pixels = 9L,
  min_peak_score = 0.35,
  min_median_score = NULL,
  min_peak_contrast = 0.15,
  min_peak_sed_anomaly = 0.15,
  high_score_threshold = 0.35,
  min_high_score_pixels = 3L,
  probable_peak_score = 0.15,
  probable_peak_contrast = 0.15,
  probable_median_contrast = 0.02,
  probable_peak_sed_anomaly = 0.1,
  clump_relax = 0.5
)
}
\arguments{
\item{clump_labels}{Integer clump-footprint matrix.}

\item{score_info}{Result from \code{\link[=compute_clump_score]{compute_clump_score()}}.}

\item{min_pixels}{Minimum number of pixels in a clump footprint.}

\item{min_peak_score}{Minimum peak combined clump score. Use \code{NULL} to skip.}

\item{min_median_score}{Minimum median combined clump score. Use \code{NULL} to skip.}

\item{min_peak_contrast}{Minimum peak local-contrast score. Use \code{NULL} to skip.}

\item{min_peak_sed_anomaly}{Minimum peak local SED-anomaly score. Use \code{NULL} to skip.}

\item{high_score_threshold}{Threshold used to count high-score pixels.}

\item{min_high_score_pixels}{Minimum number of pixels above \code{high_score_threshold}.}

\item{probable_peak_score, probable_peak_contrast, probable_median_contrast, probable_peak_sed_anomaly}{Looser thresholds used to mark candidates as \code{"probable"} clumps.
\code{probable_median_contrast} is a compactness/coherence floor that helps
reject broad galaxy-profile pieces with coherent SEDs but little local
brightness contrast.}

\item{clump_relax}{Single global permissiveness factor for the evidence
cuts, bounded to \verb{[0, 1]}. \code{0} is conservative, \code{0.5} keeps the supplied
thresholds close to their nominal values, and \code{1} is permissive. Internally
the score/contrast thresholds are rescaled smoothly around \code{0.5}.}
}
\value{
A data frame with clump evidence metrics, \code{quality}, and \code{accepted}.
}
\description{
Builds a compact evidence table for candidate clump footprints. A candidate
is marked as accepted when it is spatially resolved and has sufficient local
compactness and local SED-anomaly evidence.
}
