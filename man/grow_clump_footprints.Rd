% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_clumps.R
\name{grow_clump_footprints}
\alias{grow_clump_footprints}
\title{Grow clump seeds into compact footprints}
\usage{
grow_clump_footprints(
  clump_seeds,
  support,
  score = NULL,
  brightness = NULL,
  grow_radius = 3L,
  max_radius = 7L,
  min_pixels = 9L,
  footprint_quantile = 0.1,
  core_drop_frac = 0.2,
  sigma_threshold = 3,
  footprint_mode = c("connected", "radial"),
  connectivity = 8L
)
}
\arguments{
\item{clump_seeds}{Data frame with \code{clump_id}, \code{row}, and \code{col}.}

\item{support}{Logical foreground support mask.}

\item{score}{Optional score map used to trim each local footprint.}

\item{brightness}{Optional local-brightness/contrast map used by connected
footprints to stop growth once pixels fall back to the local background.}

\item{grow_radius}{Initial dilation radius around each seed.}

\item{max_radius}{Maximum radius tried when a clump is too small.}

\item{min_pixels}{Minimum footprint size per clump.}

\item{footprint_quantile}{Optional local score quantile used to keep only
the stronger part of each dilated footprint. Use \code{NULL} to keep all pixels.}

\item{core_drop_frac}{For \code{footprint_mode = "connected"}, keep connected
pixels above this fraction of the seed-core brightness contrast relative to
the local background.}

\item{sigma_threshold}{For \code{footprint_mode = "connected"}, keep connected
pixels above this robust local-background significance threshold. Use
\code{NULL} to disable.}

\item{footprint_mode}{Footprint-growth mode. \code{"connected"} uses the seed only
as an anchor and returns the connected high-score component, allowing
elongated or irregular clump shapes. \code{"radial"} keeps compact radius-based
growth.}

\item{connectivity}{Pixel connectivity used by \code{footprint_mode = "connected"}.}
}
\value{
A list with \code{clump_labels} and \code{footprint_summary}.
}
\description{
Grow clump seeds into compact footprints
}
