% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_regions_large.R
\name{segment_regions_large}
\alias{segment_regions_large}
\title{Segment photometric regions from large spectral cubes}
\usage{
segment_regions_large(
  input,
  Ncomp = 5,
  redshift = 0,
  pretransform = NULL,
  mask_pretransform = "none",
  cluster_pretransform = "none",
  scale_fn = median_scale,
  n_regions = NULL,
  use_starlet_mask = TRUE,
  support_method = c("starlet", "adaptive", "starlet_contourlet", "contourlet"),
  support_args = list(),
  collapse_fn = collapse_white_light,
  starlet_J = 5,
  starlet_scales = 2:5,
  include_coarse = FALSE,
  denoise_k = 2.5,
  mode = c("soft", "hard"),
  positive_only = TRUE,
  clean_mask = FALSE,
  min_mask_area = 1L,
  close_size = 1L,
  open_size = 1L,
  keep_largest = FALSE,
  mask_mode = c("na", "zero"),
  hclust_method = "ward.D2",
  knn_k = 40,
  auto_k = FALSE,
  max_k = NULL,
  feature_scale = c("none", "robust_col"),
  spatial_weight = 0,
  return_details = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{input}{3-D array or FITS-like list with \code{imDat}.}

\item{Ncomp}{Number of output regions.}

\item{redshift}{Reserved compatibility argument carried over from \code{capivara}.
Currently unused.}

\item{pretransform}{Deprecated alias for \code{cluster_pretransform}.}

\item{mask_pretransform}{Optional spectral pretransform applied before the
white-light collapse and starlet mask. This lets the segmentation mask be
built on a transformed photometric representation while leaving the
clustering stage untouched.}

\item{cluster_pretransform}{Optional spectral pretransform applied to the
valid spectra matrix before row-wise scaling and clustering. May be one of
\code{"none"}, \code{"asinh"}, \code{"log1p"}, \code{"signed_log1p"}, \code{"copula_uniform"},
\code{"copula_gaussian"}, or a custom function returning a matrix with the same
dimensions as the input.}

\item{scale_fn}{Per-spectrum scaling function applied row-wise before
clustering. Use \code{identity} for no row scaling.}

\item{n_regions}{Deprecated alias for \code{Ncomp}.}

\item{use_starlet_mask}{Logical; if \code{TRUE}, derive a photometric mask before clustering.}

\item{support_method}{Foreground support builder used when
\code{use_starlet_mask = TRUE}. Options are \code{"starlet"} (default),
\code{"adaptive"} (multi-band statistical support), \code{"starlet_contourlet"}
(directional refinement), and \code{"contourlet"} (standalone directional
support; more experimental).}

\item{support_args}{Optional named list passed to the selected support
builder. For \code{"adaptive"}, arguments are passed to
\code{\link[=build_adaptive_support]{build_adaptive_support()}}. For contourlet methods, arguments are passed to
\code{\link[=build_contourlet_mask]{build_contourlet_mask()}}.}

\item{collapse_fn}{Function used to collapse the cube to a 2-D image.}

\item{starlet_J}{Number of starlet scales.}

\item{starlet_scales}{Scales to keep when reconstructing the starlet image.}

\item{include_coarse}{Logical; include the coarse plane in the starlet reconstruction.}

\item{denoise_k}{Optional starlet denoising threshold.}

\item{mode}{Starlet thresholding mode.}

\item{positive_only}{Logical; keep only positive reconstruction values in the mask.}

\item{clean_mask}{Logical; apply optional support-mask cleanup after the
foreground support is built.}

\item{min_mask_area}{Minimum connected-component area retained when
\code{clean_mask = TRUE}.}

\item{close_size}{Binary closing brush size used when \code{clean_mask = TRUE}.
Use \code{1} to skip closing.}

\item{open_size}{Binary opening brush size used when \code{clean_mask = TRUE}.
Use \code{1} to skip opening.}

\item{keep_largest}{Logical; keep only the largest connected component after
area filtering when \code{clean_mask = TRUE}.}

\item{mask_mode}{Mask fill mode passed to \code{mask_cube()}.}

\item{hclust_method}{Linkage method passed to \code{hclust()}.}

\item{knn_k}{Number of nearest neighbours for the sparse-Ward graph.}

\item{auto_k}{Logical; if \code{TRUE}, increase \code{knn_k} when the graph remains
disconnected before the requested number of regions is reached.}

\item{max_k}{Maximum \code{k} allowed when \code{auto_k = TRUE}.}

\item{feature_scale}{Optional column-wise feature scaling after row scaling.}

\item{spatial_weight}{Optional weight for appending normalized x/y pixel
coordinates to the clustering features.}

\item{return_details}{Logical; include valid indices, features, and labels.}

\item{verbose}{Logical; print sparse-Ward progress messages.}
}
\value{
A segmentation result list compatible with \code{\link[=segment_regions]{segment_regions()}} and
downstream photometry helpers such as \code{\link[=RegionPhotometry]{RegionPhotometry()}}.
}
\description{
Large-cube version of \code{\link[=segment_regions]{segment_regions()}}. It keeps the same photometric
mask, pretransform, and output structure, but replaces exact all-pairs Ward
clustering with a kNN-restricted sparse-Ward approximation.
}
