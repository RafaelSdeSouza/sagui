% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_clumps.R
\name{segment_clumps}
\alias{segment_clumps}
\title{Segment compact SED clumps and the diffuse galaxy body}
\usage{
segment_clumps(
  input,
  Ncomp_body = 80,
  bands = NULL,
  support = NULL,
  support_info = NULL,
  support_args = list(),
  clump_search_mask = NULL,
  clump_reject_mask = NULL,
  clump_reject_fraction = 0.5,
  clump_seeds = NULL,
  n_clumps = NULL,
  max_clumps = 50L,
  score_quantile = 0.995,
  min_seed_distance = 3,
  clump_levels = 3L,
  grow_radius = 3L,
  max_radius = 7L,
  min_pixels = 9L,
  footprint_quantile = 0.1,
  core_drop_frac = 0.2,
  sigma_threshold = 3,
  footprint_mode = c("connected", "radial"),
  connectivity = 8L,
  contrast_weight = 0.5,
  sed_weight = 0.5,
  small_sigma = 0.7,
  large_sigma = 4,
  apply_quality_filter = FALSE,
  min_peak_score = 0.35,
  min_median_score = NULL,
  min_peak_contrast = 0.15,
  min_peak_sed_anomaly = 0.15,
  high_score_threshold = 0.35,
  min_high_score_pixels = 3L,
  probable_peak_score = 0.15,
  probable_peak_contrast = 0.15,
  probable_peak_sed_anomaly = 0.1,
  body_segment = TRUE,
  knn_k = 40,
  auto_k = FALSE,
  max_k = NULL,
  feature_scale = c("none", "robust_col"),
  spatial_weight = 0.1,
  cluster_pretransform = "none",
  verbose = TRUE
)
}
\arguments{
\item{input}{3-D array or FITS-like list with \code{imDat}. This should usually
be the PSF-matched cube used for segmentation.}

\item{Ncomp_body}{Number of diffuse-body regions.}

\item{bands}{Bands used for support, clump scoring, and body segmentation.}

\item{support}{Optional precomputed logical support mask.}

\item{support_info}{Optional support object, e.g. from
\code{\link[=build_adaptive_support]{build_adaptive_support()}}. If \code{NULL}, adaptive support is built.}

\item{support_args}{Named list passed to \code{\link[=build_adaptive_support]{build_adaptive_support()}} when
\code{support_info = NULL} and \code{support = NULL}.}

\item{clump_search_mask}{Optional logical mask with the same spatial
dimensions as the cube. When supplied, automatic clump seeds are searched
only inside this mask, while the diffuse body can still be segmented over
the full foreground support. Use this to target context-specific compact
structures, e.g. off-disc jellyfish knots, in-disc HII regions, or halo
compact-source candidates.}

\item{clump_reject_mask}{Optional logical mask with the same spatial
dimensions as the cube. Candidate clump footprints whose overlap with this
mask exceeds \code{clump_reject_fraction} are removed before profile splitting.
This is a generic context filter, not a science label.}

\item{clump_reject_fraction}{Maximum allowed footprint overlap with
\code{clump_reject_mask}.}

\item{clump_seeds}{Optional data frame with \code{clump_id}, \code{row}, and \code{col}.
If \code{NULL}, seeds are detected automatically.}

\item{n_clumps}{Optional exact number of automatic clump seeds.}

\item{max_clumps}{Maximum number of automatic clump seeds.}

\item{score_quantile}{Quantile threshold for automatic clump seeds.}

\item{min_seed_distance}{Minimum seed separation in pixels.}

\item{clump_levels}{Number of internal profile regions per clump.}

\item{grow_radius}{Initial clump-footprint growth radius.}

\item{max_radius}{Maximum clump-footprint growth radius.}

\item{min_pixels}{Minimum pixels per grown clump footprint.}

\item{footprint_quantile}{Local score quantile used to trim grown footprints.}

\item{core_drop_frac}{For connected footprints, keep connected pixels above
this fraction of the seed-core brightness contrast relative to the local
background.}

\item{sigma_threshold}{For connected footprints, keep connected pixels above
this robust local-background significance threshold. Use \code{NULL} to disable.}

\item{footprint_mode}{Footprint-growth mode. \code{"connected"} uses the seed only
as an anchor and returns the connected high-score component, allowing
elongated or irregular clump shapes. \code{"radial"} keeps the older compact
radius-based growth.}

\item{connectivity}{Pixel connectivity used by \code{footprint_mode = "connected"}.}

\item{contrast_weight}{Weight of compact local contrast in the clump score.}

\item{sed_weight}{Weight of local SED anomaly in the clump score.}

\item{small_sigma}{Compact smoothing scale for the clump score.}

\item{large_sigma}{Background smoothing scale for the clump score.}

\item{apply_quality_filter}{Logical; if \code{TRUE}, discard candidate clumps that
fail the evidence cuts before profile splitting.}

\item{min_peak_score, min_median_score, min_peak_contrast, min_peak_sed_anomaly}{Evidence cuts passed to \code{\link[=clump_evidence_table]{clump_evidence_table()}}.}

\item{high_score_threshold, min_high_score_pixels}{High-score-pixel criterion
passed to \code{\link[=clump_evidence_table]{clump_evidence_table()}}.}

\item{probable_peak_score, probable_peak_contrast, probable_peak_sed_anomaly}{Looser evidence thresholds used for \code{"probable"} clumps.}

\item{body_segment}{Logical; segment the remaining diffuse body.}

\item{knn_k, auto_k, max_k, feature_scale, spatial_weight}{Sparse-Ward arguments
passed to \code{\link[=segment_regions_large]{segment_regions_large()}} for the diffuse body.}

\item{cluster_pretransform}{Spectral pretransform for diffuse-body
segmentation.}

\item{verbose}{Logical; print progress messages.}
}
\value{
A Sagui-like list with merged \code{cluster_map}, clump diagnostics, and
body segmentation products.
}
\description{
Experimental clump-aware segmentation mode. The method builds a loose
foreground support, detects compact SED-anomalous seeds (or accepts supplied
clump seeds), grows them into clump footprints, splits each clump into
internal profile levels, and runs \code{\link[=segment_regions_large]{segment_regions_large()}} on the remaining
diffuse body. The final \code{cluster_map} merges clump-profile regions and the
diffuse-body segmentation.
}
