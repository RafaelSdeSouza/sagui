% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/threshold_starlet_regions.R
\name{threshold_starlet_regions}
\alias{threshold_starlet_regions}
\title{Reconstruct and threshold starlet scales to produce a region mask}
\usage{
threshold_starlet_regions(
  dec,
  keep_scales = 2:5,
  include_coarse = FALSE,
  denoise_k = 3,
  mode = c("soft", "hard"),
  threshold = c("mad", "abs"),
  k = 3.5,
  tau = NULL,
  positive_only = TRUE,
  per_scale_positive = TRUE,
  k_hi = 5,
  k_lo = 3,
  area_min = 12,
  keep_negatives = FALSE,
  ring_nbins = 8
)
}
\arguments{
\item{dec}{\code{sagui_starlet} object (or a matrix; if matrix, J inferred from \code{keep_scales})}

\item{keep_scales}{integer vector (e.g., 3:6)}

\item{include_coarse}{logical}

\item{denoise_k}{NULL or numeric (pre-recon per-scale)}

\item{mode}{"soft" or "hard" (per-scale)}

\item{threshold}{"mad" for k·MAD, or "abs" for absolute tau}

\item{k}{numeric; multiplier for MAD (unused; kept for compat)}

\item{tau}{numeric; absolute threshold when threshold == "abs"}

\item{positive_only}{logical; TRUE uses rec>thr; FALSE uses |rec|>thr}

\item{per_scale_positive}{logical; clamp each reconstructed starlet scale to
positive values before thresholding.}

\item{k_hi}{numeric; high MAD threshold used to define seed pixels.}

\item{k_lo}{numeric; low MAD threshold used to define candidate pixels.}

\item{area_min}{integer; minimum connected-component area to keep.}

\item{keep_negatives}{logical; retain negative reconstruction values.}

\item{ring_nbins}{integer; number of radial bins for background MAD.}
}
\value{
list(soft_rec, hard_rec, mask, sigma, threshold, seeds, candidate)
}
\description{
Reconstruct and threshold starlet scales to produce a region mask
}
