---
title: "Export Regional SEDs"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Export Regional SEDs}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

Measure regional SEDs on the flux cube intended for science, using the
categorical label map learned from the segmentation representation.

## Extract

```{r extract, eval=FALSE}
regional <- extract_region_sed(
  cube = science_cube,
  labels = seg$cluster_map,
  var_cube = science_variance,
  band_values = bands
)
```

`flux_long` contains one row per region and band. `flux_wide` provides one row
per region for tools that expect tabular SEDs. `flux_err`, when a variance cube
is supplied, follows the propagated regional variance.

## Write tables

```{r export, eval=FALSE}
write.csv(regional$flux_long, "regional_seds_long.csv", row.names = FALSE)
write.csv(regional$flux_wide, "regional_seds_wide.csv", row.names = FALSE)
```

Save the band axis, PSF target, support mask, and segmentation parameters with
the tables.

## Keep labels intact

Do not renumber labels between the map and the table unless the mapping is also
saved. The label numbers only identify regions.
